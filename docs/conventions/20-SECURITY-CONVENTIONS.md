# 보안 규칙

## 목적
기본적인 보안 실수를 줄인다.

## 규칙
- 비밀값(토큰, API 키, 비밀번호)을 문서나 로그에 적지 않는다.
- 외부 입력은 라우트 경계에서 반드시 검증한다.
- 공개 API와 인증 필요 API를 구분한다.
- 세션 토큰은 `Authorization: Bearer` 헤더로만 전달한다.
- 역할에 따라 접근 범위를 제한한다.

## 검증 기준
- 인증이 필요한 라우트는 미인증 요청에 401을 반환한다.
- 권한이 없는 역할은 403을 반환한다.
- 로그에 민감 정보가 남지 않는다.
