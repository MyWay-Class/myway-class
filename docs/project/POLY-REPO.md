# 모노레포 구성 가이드

## 목적
프론트, 백엔드, 공통 코드, 문서를 한 저장소에서 어떻게 나눌지 정한다.

## 구조
- `frontend/`: 사용자 화면
- `backend/`: API와 Workers
- `packages/shared/`: 공통 타입과 DTO
- `docs/`: 설계와 운영 문서

## 원칙
- 프론트와 백엔드 책임을 섞지 않는다.
- 공통 계약은 `shared`에 둔다.
- 문서는 코드보다 먼저 갱신한다.
